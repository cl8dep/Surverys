import { DatabaseModule } from './database.module';

import { DEFAULT_DATABASE_PROVIDER_NAME } from './database.providers';

export default DatabaseModule;

export { DEFAULT_DATABASE_PROVIDER_NAME };
